# Issue #23 人工接手事項

2026-09-25 重新讀取 issue 後，資料覆蓋、固定 split 與凍結結果不變。助理已完成核對、恢復候選搜尋、影片準備及來源驗證。使用者隨後逐段確認 R1–R6 均為「失誤後修正」，恢復行為的人工確認已完成；目前只剩無法追溯的工時如何驗收，仍需使用者決定。

## 既有示範已確認

[6 段確認影片](http://127.0.0.1:8831/)總長 181.2 秒。使用者已在本對話完成逐段判讀，不需再次確認。原樣回答、判讀者、存檔時間及來源關聯已另存於 [issue-23-recovery-confirmation.json](issue-23-recovery-confirmation.json)。

| 編號 | 事件記錄 | 影片長度 | 人工確認 |
| --- | --- | ---: | --- |
| R1 | 磁鐵改為銅塊 | 18.25 秒 | 失誤後修正 |
| R2 | 鐵塊改為磁鐵 | 19.30 秒 | 失誤後修正 |
| R3 | 磁線圈機器拆除，之後設定電路板 | 39.00 秒 | 失誤後修正 |
| R4 | 電路板機器拆除，之後設定磁線圈 | 49.85 秒 | 失誤後修正 |
| R5 | 銅塊改為鐵塊 | 15.95 秒 | 失誤後修正 |
| R6 | 電路板機器拆除，之後設定磁線圈 | 38.85 秒 | 失誤後修正 |

搜尋範圍是現有 train／validation 有效回合的配方產物更改事件，合併清空配方及重選操作後得到 6 個候選，全部來自 train。不是整份資料所有恢復行為的完整清單，也沒有使用 offline-test 畫面。拆除後 entity 編號可能重用，R3／R4／R6 已依 `flow_reset` 區分，沒有誤稱同一台機器直接改配方。

6 段均有新配方的後續產出，所在連續回合均 success／valid。使用者的明確回覆補齊了事件記錄無法判斷的操作意圖。R3／R6 的片段內分別有 2／1 個無效轉移，原判定保留；確認為恢復也不會把這些區間改成可訓練。這六段不能推算整份資料的恢復工時比例。

每個來源的 dataset、events、transitions checksum 已核對，3,624 張預覽來源 RGB 逐張與凍結 metadata 的 hash 相符。短片是 H.264 檢視副本，原無損資料不變。來源、事件序號、episode、時間、影像索引、checksum 與空白人工結果見 [issue-23-recovery-review.json](issue-23-recovery-review.json)。

本機檢視包位於 `data/issue23-recovery-review/`，正式候選紀錄為 `review-v2.json`，頁面與影片 checksum 收據為 `export-v4.json`。初稿、候選紀錄及檢視包原 bytes 保留，候選中的空白人工結果代表當時狀態。最新結果以另附的人工確認紀錄為準。頁面不用登入、不會傳送答案。服務若已停止，可在 repo 根目錄執行：

```powershell
.venv/Scripts/python.exe tools/serve-annotation-workbench.py data/issue23-recovery-review 8831
```

## 工時需要使用者決定

Issue 與 evaluation-v2 要求先約 3 小時初錄分析，再以其餘約 7 小時補錄，正常／恢復約 70／30 是人工工時。使用者已確認歷史工時「未紀錄」，目前沒有可復原的工時帳。再看影片或新增錄製都不能補證舊工時，也不能把 4.678 小時有效 capture 改稱人工工時。

建議採取下列**待使用者核准的驗收修訂**：

> 本批歷史人工工時與正常／恢復工時比例缺測，永久保留未知，不宣稱符合原 3＋7 小時及 70／30 安排。以既有正式驗證、20／3／3 正例、split 隔離、不可變凍結資料及另附的人工恢復確認完成本批交付；工時條款記為經同意的驗收例外。後續示範才開始分別記錄正常操作、失誤恢復與排除的休息／等待時間。此例外不修改模型門檻，也不授權訓練或 test 揭露。

這是需求擁有人才能決定的驗收例外，尚未生效。助理沒有改 issue、既有 protocol 或 data freeze。若使用者接受，需保存決議並依凍結規則另立版本／修訂關聯，保留 v2 原 bytes；不能把原版本改成「本來就符合」。

若不接受例外，就維持工時條款未通過，先決定新一輪計時示範的預算與資料版本，再進行人工錄製。不能承諾補一兩回或補足名目 10 小時便能追認歷史要求。現有正例配額已滿，沒有為湊配額而重錄的需要。

## 人工回覆後由助理接續

1. 已原樣保存 R1–R6 回答、判讀者、存檔時間及候選來源 ID；六段均獲明確確認，列為人工恢復證據。
2. 依使用者的工時決議記錄已核准的例外或新的計時計畫。若仍需補錄，先確定人類可投入的預算，再檢查當下容量、錄製指紋、校正與固定 manifest。2026-09-25 靜態檢查顯示遊戲未啟動、設定仍為第十八批 seeds；這不是下一批已備妥的宣告，也沒有修改遊戲設定。
3. 新錄製逐份走既有四檔驗證、compiler 與 #19 索引，保留失敗及合法前綴；同 manifest 重試不換 split。工時到限或容量不足就停止新增並記缺口。
4. 只在必要的資料／決議補齊後產生新的附加紀錄或新資料版本，保留所有舊凍結檔。重新核對 #23 六條條件，有未核准缺口便不結案。

Issue 沒有要求某個固定數量的完整有效失敗回合，也沒有列恢復情境種類配額。沒有死亡／超時回合這件事不另增成新的補錄門檻。既有失誤恢復行為已確認，工時條款仍未驗收。人工模型判讀、模型品質、recipe freeze 與正式代理試驗屬後續 gate，本次不要求使用者完成。

## 本次交接驗證

六段 MP4 均通過完整解碼檢查，最終檢視包的 14 個檔案 checksum、候選紀錄封印及 repo 內副本一致。瀏覽器實測 R1 播至結束並切換 R3 正常；頁面改為點選才載入影片，避免同時預載六段。當時 Standards／Spec 獨立複核均無新增待修項目。交接後新增的人工確認另行保存，沒有改產品程式；既有測試結果見 [交付核對](issue-23-status.md#本次驗證)。

人工確認存檔後，六個回答、dataset／episode ID、來源 checksum 與新舊紀錄封印均通過核對；原候選及原核對報告 bytes 與 `7b42b0f` 相同。此次 Standards／Spec 再次複核均為 0 項新增待修，工時例外仍未核准。
