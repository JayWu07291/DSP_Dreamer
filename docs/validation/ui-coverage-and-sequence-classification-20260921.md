# UI 覆蓋與五段影片分類

2026-09-22 更新：P001–P005 分類已收件，原文與下一組 P006–P015 見 [影片分類收件](sequence-classification-20260922.md)。以下保留當時準備紀錄。

Jay 回覆「3A、4A、4B、5A 都正確」，已保存為第二批人工確認。科技樹、熔爐視窗露出的上半部、配方選取視窗與矩陣研究站四個框均沿用原提案座標，完整來源與原文見 [確認收據](ui-regions-02-confirmed-20260921.json)。

目前共 15 個關鍵項目，分布於 7 張原圖：物品／數字 10、游標 2、配方 1、建築連接 2。另有 8 個 UI 框，分布於 5 張原圖：物品清單 2、合成器 2、科技 1、建築 2、研究站 1。第二批的三張圖只有 UI 範圍確認，保存在 `ui_region_reviews`，沒有替它們捏造關鍵項目。

草稿為 `runs/catalog-v4/corpus-integration-20260921/annotations-with-ui-regions-02-draft.json`。既有 15 個項目及 Q05 澄清保持不變，來源身分與 checksum 已核對。

## 協定要求

重新核對 [evaluation-v2](../../protocols/evaluation-v2.md) 及 Issue #22：固定 200 張重建圖的抽樣與逐任務配額已完成；關鍵項目的四種類型及五類 UI 已有人工確認的範例。協定沒有要求每張圖必須有關鍵項目，也沒有要求逐張確認所有可見細節。`whole_frames_reviewed` 仍誠實保留為 0，但不能額外把這個工作台欄位當成凍結門檻。

目前保存的是人工確認草稿與封存收據，尚未完成正式 `dsp-evaluation-annotations/1` 與最終評估輸入包的整體凍結。預測序列的分類、受影響範圍與關鍵狀態仍待確認，之後才能依固定規則選出四類各 50 段。模型品質未評估，訓練仍未授權。

## 首組分類的操作說明（已收件）

開啟 <http://127.0.0.1:8834/>，依序查看 P001–P005。先播放要分類的 1.5 秒；需要前因時可播放約 8 秒的前情加本題。混合操作依第 5 步畫面的主要變化分類。

| 回答 | 主要可見變化 |
| --- | --- |
| 移動／視角 | 角色位置或鏡頭改變 |
| UI | 開關面板、切換選項等介面操作 |
| 建造／物品 | 放置、拆除、建築連接、拿取或放入物品 |
| 等待 | 等待狀態，沒有上述主要操作；不要求完全靜止 |

回到對話逐行回答 `P001：類別` 至 `P005：類別`。無法判斷可寫原因，不必填座標或匯出 JSON。這批只收分類，不會把回答當成完整序列標註。

## 準備與核對

[準備腳本](prepare-sequence-classification-01.py) 沿用已匯出候選佇列前五筆，沒有依內容另挑片段。題包 ID 為 `b0374d5b7341ccdf9477b5afcf6ed0762d6968ffabdcaa2878deaf5fa69243df`；來源與檔案 checksum 見 [準備收據](sequence-classification-01-prepared-20260921.json)。每段保留三張無損原圖，MP4 僅用於觀看前後變化。3 部 MP4 與 15 張原圖使用 hard link 沿用既有儲存，媒體複製量為 0。

腳本核對來源 seal、checksum、路徑範圍、五個時間區間及 hard link 身分，並檢查五部播放器與十五張原圖。mypy 通過；新增 UI 草稿通過既有 Node 匯入驗證。瀏覽器已檢查影片定位、播放及區間結束暫停；影片播放時間以瀏覽器事件為準，精確時點以無損原圖為準。本次未開啟歷史 dataset、重建總索引或修改應用程式。

## Standards

獨立複核無可操作發現。沿用既有 seal／checksum 與本機影片伺服器，未新增依賴；題包保持待確認，未預填答案。

## Spec

獨立複核無可操作發現。UI 確認只更新指定範圍；固定 P001–P005 的時間沿用匯出紀錄，前情 6.4 秒，後續區間 1.55 秒以包含第 15 步。分類不等於完整序列標註，未宣稱 200 段完成或授權訓練。
