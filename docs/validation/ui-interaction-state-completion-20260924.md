# UI 與建造／物品的範圍及末幀狀態

Jay 在 8857 頁回覆「77 題的兩項都正確」。已逐列保存本批範圍與第 15 步狀態確認，原先的分類答覆、來源、時間與人工不確定項目均保留。前 37 列正式候選未變，新接入 77 列，共 114 列完整候選。

| 分類 | 已確認分類 | 完整候選 | 本頁待核對範圍與末幀 |
| --- | ---: | ---: | ---: |
| 移動／視角 | 50 | 50 | 0 |
| UI | 63 | 0 | 63 |
| 建造／物品 | 64 | 0 | 64 |
| 等待 | 64 | 64 | 0 |

累計 242 段收到回答或確認，241 段已分類，P085 仍待判定。等待多出的 14 段屬額外候選，因此目前滿足最終配額的是兩類各 50，共 100／200；不是最終 200 已確認 114。

## 代理整理

使用現有 frozen workbench 的起點、第 5 步及第 15 步 PNG，逐一查看剩餘 127 組。以實際末幀描述游標所指位置、選用配方或可讀數字，沒有以先前操作意圖推定建築已完成或連線已接通。

局部範圍是依原圖提出的人工可修正建議，保存未展開外框，再四邊加 8 pixel、裁切至 640×360，尺寸不足時擴至至少 64×64。鏡頭或場景建造格線全面改變者提出全畫面。這不是逐像素追蹤全部影格所得的精確框；頁面提供起點、第 5 步、第 15 步的同一框，以及完整歷史／未來影片，供核對是否遺漏或過大。所有新提案 `reviewed=false`，不以舊分類確認代替新欄位確認。

分類與末幀狀態的時間不同：仍保留第 5 步的人工作答，沒有依末幀內容重新分類。原 400 題、補充 947 個候選、既有來源與尚未處理資料都保留。未變更 protocol、index、evaluation inputs、seed 2201 或完整正式列的排序規則；未凍結抽樣或授權訓練。

本次沒有開啟 dataset、parquet 或 RGB archive，沒有重建索引、複製或重新編碼媒體。新頁使用既有原圖與影片的 hard link，框由 CSS 疊加，不改原圖 bytes。

## 人工接手

[合併核對頁](http://127.0.0.1:8858/) 有 UI 63 題、建造／物品 64 題，共 127 題，直接顯示末幀與預填描述。只核對兩項：青框範圍、末幀描述。可展開原圖與影片，無需再寫操作過程、座標或匯出 JSON。

全數符合可回覆「127 題的兩項都正確」；可分次確認 UI 或建造／物品，或只列有誤的題號、項次、修正。若有不確定，保留不確定。沒有新增錄製工作。

全數通過後，完整候選將為 241 列，再由代理執行既定的四類各 50 段抽樣與資料凍結檢查。正式試驗前的舊輸入設定 hash 差異、模型品質驗證及訓練授權仍另行處理。

## 驗證與收據

- [77 題確認收據](sequence-state-completion-01-confirmed-20260924.json)
- [127 題提案資料](ui-interaction-state-proposals-20260924.json)
- [交付收據](sequence-state-completion-02-prepared-20260924.json)
- [產生器與檢查](prepare-ui-interaction-state-completion.py)：`.venv/Scripts/python.exe docs/validation/prepare-ui-interaction-state-completion.py --check`

檢查通過：77 列只提升範圍與狀態、原分類敘述保留、其餘 165 列不變、舊 37 列正式候選不變、新 77 列依原 task_id 接入、總數 114、127 新提案待確認、padding／clipping／邊界最小尺寸、seal、來源 hash、hard link、381 原圖及青框、127 組可展開影片。

本機重啟命令：`.venv/Scripts/python.exe tools/serve-annotation-workbench.py runs/catalog-v4/corpus-integration-20260921/sequence-state-completion-02 8858`。

完整 pytest 執行 158 項：157 項通過，原生控制測試的 EXE 被沙箱下的 Windows 應用程式控制阻擋（WinError 4551）；同一測試在獲准的沙箱外重跑後通過，無程式修改。mypy 的 18 個來源檔案通過。

瀏覽器確認 127 題、381 張原圖及 381 個青框；窄版頁面正常顯示，前情可展開／收起。P002 三張原圖均載入為 640×360，短片實播後自動停在 29.589 秒（終點 29.55 秒），沒有影片錯誤。頁面保留在 8858 供人工接手。

## Standards

獨立複核：硬性違規 0、可行動的 Fowler smell 0。原文與來源保留、確認欄位限定、seal、hash、hard link 及邊界處理通過。單次準備器沒有需要另抽象或改動舊封存工具的問題。

## Spec

獨立複核：缺漏、範圍擴張或錯誤實作 0。77 列只提升新欄位；127 列提案依原圖提出並仍需人工核對；未改分類、抽樣規則或授權訓練。預填內容不視為已確認標註。

Standards 0 項、Spec 0 項發現。
