# 第三批錄製核對

2026-09-17，使用者提交兩個完整回合，並說明數字 3 是物流建築分類快捷鍵，選擇不再擴充動作空間。本次維持 `action_catalog_v4` 的 21 維控制、原有效性規則及既有校正，不新增按鍵、不把 Alpha3 改成其他動作，也不刪除原始輸入。

錄製來源為 `runs/live/b5745e3e-6c0e-404f-aa3d-8ce7af871f58.source.evidence`，正式資料集位於相鄰的 `.source.dataset`，共 13,434 張影像、13,433 個轉移。錄製器已發布完成，原始三個檔案的 checksum 及 dataset 的正式 loader 讀回均通過。

| 回合 | 遊戲結果 | 資料判定 | 有效轉移 |
| --- | --- | --- | --- |
| 第一回合，約 5 分 31 秒 | 成功 | valid，完整回合可用 | 6,570 |
| 第二回合，約 5 分 42 秒 | 成功 | invalid，保留合法前綴 | 4,314 |

第二回合約 215.85 秒出現 Alpha3。由於目前控制清單沒有此鍵，現行契約會排除該輸入及之後的訓練轉移，回合的遊戲結果仍保存為 success。可用前綴仍能貢獻合法 sequence 與任務正例，不能將它算成完整有效回合，也不能把未表達的輸入當作模型沒有操作。

本批沿用 demonstration plan 的 index 2，manifest `a980b35ca42b710f969c8d4f203e38b6c04df4f0b5f1adb9955c6e4f92b78c51`、bucket 19，屬於 train。錄製使用已核准的新指紋與校正，progress version 為 4、diagnostic mode 為 false。

## 索引結果

七份來源共 124,849 張影像已完成核對。新索引 ID 為 `e5adc360b0c7abe01eb35c6fe5052242efa1a89cf1e2ec14c3ed3744a7178491`，完整有效回合為 train 12、validation 1、offline-test 0；train 比上批增加 1 回，完整有效示範約 1.166 小時。

第一回合提供 task 1–13 及 15 的合法 active 正例；第二回合的前綴提供 task 1–9。鐵、銅採礦機各增加 2 回正例，熔爐連接增加 1 回。啟動拆解與電路板產線本批沒有新增合法 active 正例；不能把背景完成算成 active reward。各任務剩餘缺口見 [機器可讀核對報告](recording-batch3.json)。

評估輸入包 ID 為 `6fb136816e447e92ac688a2bfbf868a915bef42869ef305741f2990e0ab21c67`。重建抽樣名單與上一版相同，既有人工草稿保留；逐任務圖片配額仍缺 32 張，預測分類與完整標註仍待人工處理。Train-only baseline 維持 ready，資料覆蓋 gate 仍為 false。索引、輸入包、來源檔案 checksum 與完整審查紀錄在 `runs/catalog-v4/recording-review-20260917-batch3`。

## 後續操作

不要求重錄這兩回合。第四批已按既有登錄順序套用 index 3，seeds 為 230009／230010／230011，manifest `7c431719b953aa47a0398e3f70e8784c4cbf22c0fe46994fd67b4fa0881d7cb9`，固定 bucket 66 屬於 train。已核對保留 trial 隔離、存檔與輸入設定，且只修改三個 seeds；原設定、準備與套用收據均保存在核對目錄。動作空間、DLL、指紋、校正及 Diagnostics 設定保持不變，不需重啟或再按 F6。

下一步仍為兩個完整回合：F8 開始，成功後 F9 重載，再成功後 F8 結束並等發布完成。沿用採礦優先安排。後續錄製若要選物流建築分類，改用滑鼠點選，讓實際操作能完整表達在目前的動作空間中。

資料覆蓋、正式人工標註與模型評估仍未完成，沒有啟動訓練。本輪未修改動作契約、錄製器或評估協定；驗證針對實際錄製、索引與設定，不以重跑單元測試替代實機證據。
