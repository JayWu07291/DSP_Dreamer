# 既有分類的範圍與狀態補齊

Jay 在 8856 頁回覆「27 題的三項都正」。依本頁與前文視為「27 題的三項都正確」，原話與此解讀分別保存，沒有改寫原始答覆。只確認本批 27 題的分類、範圍、第 15 步狀態。

| 分類 | 已確認分類 | 三項完整候選 | 本輪待核對 |
| --- | ---: | ---: | ---: |
| 移動／視角 | 50 | 37 | 13 |
| UI | 63 | 0 | 0 |
| 建造／物品 | 64 | 0 | 0 |
| 等待 | 64 | 0 | 64 |

共有 242 段人工作答或確認、241 段已分類；P085 維持待判定。四類分類均足額，完整候選目前 37 段。已將新 27 列依各自來源的 task_id 接入正式 schema 的候選片段，前 215 列原始紀錄與既有 10 列正式候選不變。未凍結最終抽樣、未修改 protocol／index／evaluation inputs 身分，也沒有授權訓練。

## 本輪代理工作

原先已分類的 13 段 movement 和 64 段 waiting 都在既有原圖包中。代理逐一讀取 77 張原生第 15 步原圖，另核對 13 段 movement 的起點／第 5 步原圖，確認相機造成場景位移。movement 因鏡頭移動、waiting 依既定協定，均提出全畫面範圍。末幀狀態以可辨認的游標所指位置為主，另標註五項配方與一項物品／數字。

這些是代理建議，`reviewed=false`；既有 `classification_reviewed=true` 只表示分類早已確認，不能當成新範圍或狀態已經確認。77 列與人工彙整來源逐列相符，P085 與其他類別均未修改。過渡片段的第 15 步可能開始下一個操作，保留第 5 步分類，不依末幀 UI 重新分類。

本輪沒有讀取 dataset、parquet 或 RGB archive，只核對既有 PNG、影片及小型收據。所有頁面媒體以 hard link 共用現有檔案；沒有新增錄製、重新編碼、複製影片或重建語料。

## 人工接手

在 [範圍與末幀狀態核對頁](http://127.0.0.1:8857/) 看每題兩項新增內容。分類只顯示供參考，不用重答。頁面直接顯示末幀原圖，起點、第 5 步與影片收在可展開區，圖可點開放大。

若全部符合，回覆「77 題的兩項都正確」。有誤只列題號、項次及修正，不確定可以保留；也可分次回覆已確認的題號。無需重寫操作過程、填座標或補錄。

77 題全數通過後，完整候選為 movement 50、waiting 64，共 114 列。這足以供兩類各固定抽樣 50 列，不能當成最終 200 列已完成 114 列，waiting 的 14 列為額外候選。UI 63 及 interaction 64 的範圍、關鍵狀態仍待代理整理，再交付核對。

## 核對

- [27 題確認收據](movement-supplement-01-confirmed-20260924.json)
- [77 題提案收據](sequence-state-completion-01-prepared-20260924.json)
- [產生器與可重跑檢查](prepare-sequence-state-completion.py)，檢查命令 `.venv/Scripts/python.exe docs/validation/prepare-sequence-state-completion.py --check`

檢查已通過：原話與解讀、舊 215 列不變、新 27 列精確提升、37 列正式候選、77 列原分類不變且範圍／狀態待確認、seal、媒體 checksum、hard link、頁面 77 張末幀圖與 77 組可展開前情。

重啟本機頁面：`.venv/Scripts/python.exe tools/serve-annotation-workbench.py runs/catalog-v4/corpus-integration-20260921/sequence-state-completion-01 8857`。

瀏覽器已確認 77 題與 231 張原圖引用，前情可展開及收起。P021 短片實播後停在 127.594 秒，區間終點為 127.55 秒，640×360、無影片錯誤；該題三張原圖均正常載入。完整測試 158 項通過，耗時 182.06 秒；mypy 的 18 個來源檔案通過。

## Standards

獨立複核：硬性違規 0、可行動的 Fowler smell 0。原話與解讀、215 列保留、37 列完整候選、77 列待確認、seal、來源鏈及 hard link 均通過。分類、凍結與訓練授權狀態沒有混淆；頁面使用末幀直顯與原生 details，沒有引入新框架。

## Spec

獨立複核：缺漏、範圍擴張或錯誤實作 0。依 §41 保留第 5 步人工分類，§43 的 waiting 使用全畫面並另填第 15 步狀態；提案仍需 Jay 核對，最終 waiting 分母與結果另報。只使用 frozen workbench 原始證據，沒有模型輸出選樣或最終抽樣凍結。

Standards 0 項、Spec 0 項發現。
