# 任務身分與排程優先序分開

2026-09-14，使用者依 #21 十一次連續流程的分析，確認改用採礦優先的安排。`progress_version=4` 保留 task ID、17 維條件與全部依賴，優先序改為 `0,1,2,3,4,6,7,5,8,9,10,11,12,13,14,15`；等待仍為 16，active 不搶占，提前完成仍保留。這項確認修訂 #12「ID 同時是優先順序」的條款，其餘節點 predicate、科技背景進度與 gate 不變。

v1–v3 按原規則讀取，不就地改寫歷史標籤。完成判定相容的 v3 dataset 可另存 v4 衍生資料，保留原始事件、影像、動作、split 與有效性，只重算 task、next task、條件及 scalar reward；原始事件裡的 v3 宣告仍作來源證據。v1／v2 缺少相同的產線判定，不能藉這個轉換升級。索引拒絕混合 v4 與舊排程，新的資料與評估輸入各自取得新身分。既有凍結協定與舊版輸入包保留。
