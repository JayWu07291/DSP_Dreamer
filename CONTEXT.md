# DSP Dreamer

本專題研究能在《戴森球計畫》前期受控場景中學習多個目標的小型 Dreamer 4-inspired 代理。

## Language

**微任務**:
代理從開局連續遊玩時依序追求的單一進度目標，其成功條件可由遊戲狀態判定，但不要求擁有獨立存檔或獨立試驗。
_Avoid_: 操作步驟、流程段落、小關卡

**多任務代理**:
同一個模型依指定的微任務改變行為，並分別接受各微任務的成功率評估。
_Avoid_: 多個模型、完整遊戲代理

**微任務圖**:
以先決條件連接微任務與背景里程碑的有向圖；外部排程器每次只從已解鎖的微任務中指定一項給代理，背景里程碑可在其他行為進行時完成。
_Avoid_: 固定操作腳本、多個同時生效的提示、模型自行規劃

**提示序列**:
一次完整遊玩中，外部排程器依微任務圖與固定優先規則動態產生的單一 `task_id` 流程。
_Avoid_: 預先固定的完整順序、多個同時生效的提示、模型自行規劃、自然語言任務輸入

**任務條件**:
每個決策步驟提供給代理的 active `task_id` one-hot，經 embedding 輸入 agent tokens；它由有完成事件的微任務與無 reward 的等待狀態組成，背景里程碑不佔用 task ID。任務的中文名稱只是人類可讀的紀錄。
_Avoid_: 全域目標文字、自然語言微任務、同時生效的多個 task ID

**自動化電磁矩陣產線**:
在相關科技解鎖後，以固定配方的熔煉與製造支線自動供應矩陣研究站，並由研究站產出電磁矩陣。科技尚未解鎖時的手動取放物品不屬於這條產線，但仍是有效的前期行為。
_Avoid_: 只有研究站自動生產、全程禁止手動搬運、重複改配方的單機產線

**低階鍵鼠動作**:
直接對應玩家鍵盤、滑鼠按鍵、滑鼠移動與滾輪的控制，不包含「研究科技」或「放置建築」等遊戲巨集。
_Avoid_: 原始事件、巨集動作、高階動作

**基準場景**:
代理開始一次完整遊玩的受控開局狀態，允許事先定義的小幅隨機變化。
_Avoid_: 地圖、存檔、任意開局

**錄製工作階段**:
錄製器一次持續執行的期間，可跨越遊戲重新載入並包含多次試驗嘗試與連續回合。
_Avoid_: episode、單次嘗試、單一微任務

**試驗嘗試**:
針對同一份試驗 manifest 啟動的一次執行；人工介入或其他無效原因可使該次嘗試失效，之後仍以同一份 manifest 重試。
_Avoid_: 錄製工作階段、資料切分、不同試驗設定

**連續回合**:
從基準場景擾動完成後第一個可控制且 RGB 有效的觀測開始，到最終成功或回合終止原因發生為止的一段連續遊玩；微任務切換不會結束回合。
_Avoid_: `GameMain.Begin`、單一微任務、獨立任務存檔、中途重設

**閉迴路試驗**:
代理反覆讀取真實遊戲的最新觀測並送出下一個動作，完成一個連續回合的一次評估；微任務成功只會推進提示序列，不會結束試驗。
_Avoid_: 動作回放、離線預測、世界模型 rollout

**錄製證據**:
錄製器保存且不覆寫的畫面、DSP 實際讀取的輸入、送出的控制要求、遊戲事件與時間資料，用來重建後續資料表示。
_Avoid_: 模型 tensor、訓練 transition、只保留量化後動作

**轉移資料集**:
由錄製證據依固定時間契約編譯出的相鄰觀測、動作區間、任務、reward、事件與終止狀態資料。
_Avoid_: 錄製器原始輸出、模型專用 tensor、動作回放

**模型視圖**:
資料載入器從轉移資料集產生、實際提供給模型的觀測、動作、任務與訓練目標表示。
_Avoid_: 錄製證據、正式資料集 schema

**觀測**:
代理在一個決策步驟取得的 640×360 RGB 遊戲畫面，包含完整 UI 與合成後游標，不包含具特權的遊戲內部狀態。
_Avoid_: RGBA 錄製緩衝區、狀態快照、去除 UI 的畫面

**實際動作**:
一個相鄰觀測區間內 DSP 實際讀取的低階鍵鼠輸入，是訓練與動態模型使用的動作依據。
_Avoid_: 控制要求、Windows 原始封包、模型量化類別

**控制要求**:
閉迴路控制器要求注入遊戲的低階鍵鼠動作，用於核對延遲、校正與注入失敗，不取代 DSP 實際讀取的動作。
_Avoid_: 實際動作、巨集動作、訓練標籤

**模型動作**:
模型視圖中的版本化低階控制表示，由固定順序的二元按鍵、量化滑鼠位移與滾輪類別組成。
_Avoid_: 錄製器原始事件、連續滑鼠回歸、遊戲巨集

**遊戲事件**:
從遊戲內部狀態觀察到的事實，不直接代表微任務完成或代理取得 reward。
_Avoid_: 節點完成、reward、task ID

**節點完成**:
微任務或背景里程碑的版本化判定條件第一次由 false 轉為 true；同一動作區間可完成多個節點。
_Avoid_: 原始遊戲事件、重複進度通知、只有 active 微任務的 scalar reward

**回合結果**:
連續回合在遊戲語意上的結束結果，包括成功、死亡、超時或無法繼續；它不表示錄製資料是否可信。
_Avoid_: 資料有效性、人工介入、錄製器故障

**資料有效性**:
錄製資料是否可供評估或訓練使用的獨立判定；無效回合仍可保存錄製證據，訓練也可只使用故障前已驗證的有效前綴。
_Avoid_: 回合成功率、微任務完成、terminal reward

**任務相關片段**:
完整遊玩資料中包含至少一個節點完成的合法訓練 sequence，用來提高稀少關鍵操作在訓練抽樣中的比例。
_Avoid_: 獨立任務存檔、完整 episode

**進度片段**:
包含微任務進展事件但沒有節點完成的合法訓練 sequence，只供分析或另行設計的消融實驗使用。
_Avoid_: 預設任務相關片段、獨立任務存檔、完整 episode
